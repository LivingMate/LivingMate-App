/*

import React from 'react';
import { View } from 'react-native';
import { SvgXml } from 'react-native-svg';
import customIconSvg from '../assets/icons/customIcon.svg';

interface CustomIconProps {
    width?: number;
    height?: number;
    color?: string;
  }
  
const iconWithSize: CustomIconProps = {
    width: 24,
    height: 24,
};
  
const iconWithoutSize: CustomIconProps = {};
  
const CustomIcon: React.FunctionComponent<CustomIconProps> = ({ width = 24, height = 24 }) => {
    return (
      <View>
        <SvgXml xml={customIconSvg} width={width} height={height} />
        {/* 이 외에 필요한 커스텀 아이콘 로직 추가 
      </View>
    );
};

export default CustomIcon;
*/